# Copilot Instructions

## Project Guidelines
- User prefers not to keep duplicate repository methods for the same behavior (e.g., avoid both FindByIdAsync and GetByIdAsync for customers; use a single GetByIdAsync).